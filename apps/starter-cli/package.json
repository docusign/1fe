{
  "name": "starter-cli",
  "version": "1.0.0",
  "description": "Example usage where a UI platform team published an abstracted 1FE CLI instance for their company to consume",
  "main": "./dist/index.js",
  "bin": {
    "consuming-organization-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "type:check": "tsc --noEmit --skipLibcheck"
  },
  "dependencies": {
    "@devhub/cli": "^1.0.0"
  },
  "devDependencies": {
    "find-cache-dir": "^5.0.0"
  }
}
