/**
 * This babel configuration is only used to handle code transpiling in Jest.
 * The .babelrc file is used for transpiling files during the build process.
 */
module.exports = {
  presets: ['@babel/preset-env'],
};
